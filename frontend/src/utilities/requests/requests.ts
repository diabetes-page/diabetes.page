/**
 * This file was autogenerated using the route definitions from the backend.
 */
import { AxiosResponse } from 'axios';
import { OfficialMessageResource } from '../../../../backend/src/domains/appointments/resources/OfficialMessageResource';
import { Resource as CreateAppointmentResource } from '../../../../backend/src/domains/appointments/routes/createAppointment/Resource';
import { Resource as IndexAppointmentsForUserResource } from '../../../../backend/src/domains/appointments/routes/indexAppointmentsForUser/Resource';
import { Resource as ShowConferenceDataResource } from '../../../../backend/src/domains/appointments/routes/showConferenceData/Resource';
import { Resource as CheckAuthStatusResource } from '../../../../backend/src/domains/auth/routes/checkAuthStatus/Resource';
import { Resource as LoginResource } from '../../../../backend/src/domains/auth/routes/login/Resource';
import { Resource as RegisterResource } from '../../../../backend/src/domains/auth/routes/register/Resource';
import { SensitiveDataUserResource } from '../../../../backend/src/domains/users/resources/SensitiveDataUserResource';
import { Resource as IndexUsersResource } from '../../../../backend/src/domains/users/routes/indexUsers/Resource';
import { Get, Post, Put, withAuth } from './axios';

type SwitchConferenceSlideParameters = {
  presentationIndex: number;
};
type LoginParameters = {
  email: string;
  password: string;
};
type RegisterParameters = {
  email: string;
  password: string;
  name: string;
};

export const requests = {
  createAppointment: async (): Promise<
    AxiosResponse<CreateAppointmentResource>
  > => Post(`/appointments`, await withAuth()),

  indexAppointmentsForUser: async (
    id: number,
  ): Promise<AxiosResponse<IndexAppointmentsForUserResource>> =>
    Get(`/users/${id}/appointments`, await withAuth()),

  showConferenceData: async (
    id: number,
  ): Promise<AxiosResponse<ShowConferenceDataResource>> =>
    Get(`/appointments/${id}/conference`, await withAuth()),

  switchConferenceSlide: async (
    id: number,
    data: SwitchConferenceSlideParameters,
  ): Promise<AxiosResponse<OfficialMessageResource>> =>
    Put(`/appointments/${id}/conference/slide`, data, await withAuth()),

  checkAuthStatus: async (): Promise<AxiosResponse<CheckAuthStatusResource>> =>
    Get(`/auth/status`, await withAuth()),

  login: async (data: LoginParameters): Promise<AxiosResponse<LoginResource>> =>
    Post(`/auth/login`, data, await withAuth()),

  register: async (
    data: RegisterParameters,
  ): Promise<AxiosResponse<RegisterResource>> =>
    Post(`/auth/register`, data, await withAuth()),

  indexUsers: async (): Promise<AxiosResponse<IndexUsersResource>> =>
    Get(`/users`, await withAuth()),

  showUser: async (
    id: number,
  ): Promise<AxiosResponse<SensitiveDataUserResource>> =>
    Get(`/users/${id}`, await withAuth()),
};
